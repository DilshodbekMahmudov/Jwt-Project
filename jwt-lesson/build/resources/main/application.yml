server:
  port: 8084
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/Jwt
    username: postgres
    password: dilshod
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect


